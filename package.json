{
  "name": "hono-todo-api-with-bun",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "lint": "bunx @biomejs/biome check --apply src tests"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.3",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "4.1.5"
  }
}
